const {
    TOTAL_PHONES,
    MOCK_PHONES,
    phoneResolvers,
    PhoneTypeDefs,
} = require('./phone');

module.exports = {
    PHONE: { TOTAL_PHONES, MOCK_PHONES, phoneResolvers, PhoneTypeDefs },
};
